import styled from 'styled-components';

const ChatHeaderTemplate = styled.div`
  padding: 0 30px 0 20px;
  font-weight: bold;
  min-height: 64px;
  border-bottom: 1px solid rgba(0, 0, 0, 0.15);
`;

export default ChatHeaderTemplate;
